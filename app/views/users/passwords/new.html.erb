<div>
<% content_for :title, "Recupera tu contraseña" %>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }, html: { :class => "cd-form floating-labels" }) do |f| %>
  <%= devise_error_messages! %>
	
<fieldset>
    <legend>Recuperar Contraseña</legend>

  <div class="icon">
    <%= f.label :email, "Correo electrónico", :class => "cd-label" %>
    <%= f.email_field :email, autofocus: true,  :class => "email", :required => true %>
  </div>

  <div class="actions">
    <%= f.submit "Recuperar contraseña" %>
  </div>
  <%= render "users/shared/links" %>
<% end %>
</div>